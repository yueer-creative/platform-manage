// const path = require('path')

module.exports = {
//   output: {
//     path: path.resolve(__dirname, 'bundle')
//   }
  outputDir: 'bundle',
  devServer: {
    port: 8011, // 启动端口
    open: true // 启动后是否自动打开网页
  }
}
